<p id="notice"><%= notice %></p>

<h1>Styles</h1>

<table>
  <tbody>
    <% @styles.each do |style| %>
      <tr>
        <td><%= link_to style.name, style %></td>
      </tr>
    <% end %>
  </tbody>
</table>
