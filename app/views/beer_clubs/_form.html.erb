<%= form_with(model: beer_club, local: true) do |form| %>
  <% if beer_club.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(beer_club.errors.count, "error") %> prohibited this beer_club from being saved:</h2>

      <ul>
      <% beer_club.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <form>
  <div class="form-group row">
    <label for="beer_club_name" class="col-sm-2 col-form-label">Name</label>
    <div class="col-sm-10">
      <input type="text" class="form-control" id="beer_club_name" name="beer_club[name]">
    </div>
  </div>
  <div class="form-group row">
    <label for="beer_club_founded" class="col-sm-2 col-form-label">Founded</label>
    <div class="col-sm-10">
      <input type="number" class="form-control" id="beer_club_founded" name="beer_club[founded]">
    </div>
  </div>
  <div class="form-group row">
    <label for="beer_club_city" class="col-sm-2 col-form-label">City</label>
    <div class="col-sm-10">
      <input type="text" class="form-control" id="beer_club_city" name="beer_club[city]">
    </div>
  </div>
  <button type="submit" class="btn btn-primary">Save beer club</button>
  </form>
<% end %>
